AMQP_URI: !env_var "pyamqp://${RABBIT_USER}:${RABBIT_PASSWORD}@${RABBIT_HOST:127.0.0.1}:${RABBIT_PORT}"
max_workers: 10
parent_calls_tracked: 10

LOGGING:
  version: 1
  handlers:
    console:
      class: logging.StreamHandler
      formatter: default
      level: INFO
    filelogger:
      class: logging.handlers.RotatingFileHandler
      filename: /usr/src/logs/data.log
      backupCount: 3
      maxBytes: 1000000
      formatter: default
      level: DEBUG
  loggers:
    standardlog:
      handlers:
        - filelogger
  formatters:
    default:
      format: '[%(asctime)s %(levelname)-8s%(filename)15s:%(lineno)d]%(funcName)25s() - %(message)s'
      datefmt: '%Y-%m-%d %H:%M:%S'
  root:
    handlers: [console]
